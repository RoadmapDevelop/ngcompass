import { PresetConfig } from "@ngcompass/common";

export const ssrPreset: PresetConfig = {
    name: 'ngcompass:ssr',
    description: 'Rules for Angular Universal and SSR safety',
    rules: {},
};
