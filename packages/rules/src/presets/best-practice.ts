import { PresetConfig } from "@ngcompass/common";

export const bestPracticePreset: PresetConfig = {
    name: 'ngcompass:best-practice',
    description: 'Angular best practice rules',
    rules: {},
};
