/**
 * @fileoverview
 * Public entry for the `ngcompass` package. Re-exports `run()` from the
 * binary so the package can be invoked programmatically (e.g. from tests).
 */

export { run } from './bin/ngcompass.js';
