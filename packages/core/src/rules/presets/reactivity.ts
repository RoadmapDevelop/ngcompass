import type { PresetConfig } from '../types.js';

export const reactivityPreset: PresetConfig = {
    name: 'ngcompass:reactivity',
    description: 'Rules for signals and RxJS reactivity',
    rules: {},
};
