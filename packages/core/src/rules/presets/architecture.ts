import type { PresetConfig } from '../types.js';

export const architecturePreset: PresetConfig = {
    name: 'ngcompass:architecture',
    description: 'Rules for maintaining clean application architecture',
    rules: {},
};
